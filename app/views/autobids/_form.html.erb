<%= form_for(@autobid) do |f| %>
  <% if @autobid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@autobid.errors.count, "error") %> prohibited this autobid from being saved:</h2>

      <ul>
      <% @autobid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
